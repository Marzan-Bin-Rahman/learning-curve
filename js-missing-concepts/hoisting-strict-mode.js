'use strict'

// use strict inside quotation will prevent codes from not being secure

x=20;
console.log(x);

// this portion will work if there is no strict mode and hoisting is mainly using a variable without declaring it and only for var